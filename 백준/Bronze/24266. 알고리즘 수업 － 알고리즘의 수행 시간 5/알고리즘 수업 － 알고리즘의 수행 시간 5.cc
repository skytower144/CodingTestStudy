#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int main()
{
    ios::sync_with_stdio(false); cin.tie(0);

    unsigned long long n;
    cin >> n;

    unsigned long long result = n * n * n;

    cout << n * n * n << '\n';
    cout << 3;

    return 0;
}