//규칙에 따라 문자에 대응하는 수를 출력하는 문제
#include<stdio.h>
#include<string.h>

int main()
{
	char input[16];
	scanf("%s", input);
	int len = strlen(input);
	int sum = 0;
	
	for(int x=0; x<len; x++)
	{
		switch(input[x])
		{
			case 'A':						// case 'A' ... 'C': 
			case 'B':
			case 'C': 
				sum += 3;
				break;
			case 'D':
			case 'E':
			case 'F':
				sum += 4;
				break;
			case 'G':
			case 'H':
			case 'I':
				sum += 5;
				break;
			case 'J':
			case 'K':
			case 'L':
				sum += 6;
				break;
			case 'M':
			case 'N':
			case 'O':
				sum += 7;
				break;
			case 'P':
			case 'Q':
			case 'R':
			case 'S':
				sum += 8;
				break;
			case 'T':
			case 'U':
			case 'V':
				sum += 9;
				break;
			case 'W':
			case 'X':
			case 'Y':
			case 'Z':
				sum += 10;
				break;
		}
	}
	printf("%d\n", sum);
}
-----------------------------------------------------------------------------------------------
#include<stdio.h>
#include<string.h>

int return_num(char ch);

int main(void)
{
	char str[100];
	int i, res, sum = 0;

	scanf("%s", str);

	for (i = 0; i < (int)strlen(str); i++)
	{
		res = return_num(str[i]);
		sum += res + 1;
	}

	printf("%d\n", sum);

	return 0;
}

int return_num(char ch)
{
	if (ch >= 'A' && ch <= 'C')
		return 2;
	else if (ch <= 'F')
		return 3;
	else if (ch <= 'I')
		return 4;
	else if (ch <= 'L')
		return 5;
	else if (ch <= 'O')
		return 6;
	else if (ch <= 'S')
		return 7;
	else if (ch <= 'V')
		return 8;
	else
		return 9;
}
-------------------------------------------------------------------------------------------------------------
#include <stdio.h>

int main(void)
{
    int alpha[26]={3,3,3,4,4,4,5,5,5,6,6,6,7,7,7,8,8,8,8,9,9,9,10,10,10,10};
    char str[16];
    int sum=0;

    scanf("%s",str);
    for(int i=0;str[i];i++)
        sum += alpha[str[i]-65];
    printf("%d\n",sum);
}
