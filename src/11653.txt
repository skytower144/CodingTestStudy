//소인수분해
#include<stdio.h>

int main(void)
{
	int number;
	scanf("%d", &number);
	if(number == 1)
		return 0;
	for(int i=2; number != 1; )
	{
		if(number % i == 0)
		{
			printf("%d\n", i);
			number /= i;
		}
		else
			i++;
	}
	
	return 0;
}


// i<=number; 의 방식도 있다..