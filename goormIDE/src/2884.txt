// 알람 시계
#include <stdio.h>

int main()
{
	int total, setTime, h, m, H, M;
	scanf("%d %d", &h, &m);
	
	total = h*60 + m;
	setTime = total - 45;
	if (setTime < 0)
	{
		setTime += 1440;
	}
	H = setTime / 60;
	M = setTime % 60;
	
	
	printf("%d %d", H, M);
	
	return 0;
}